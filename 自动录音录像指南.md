# 🎬 自动录音录像使用指南

## 📋 概述

`start_auto_recording.sh` 是一个增强版启动脚本，可以在系统启动时自动开启以下功能：

1. ✅ **自动启动语音识别** - 无需手动点击录音按钮
2. ✅ **自动启动OBS** - 如果OBS未运行
3. ✅ **自动开启OBS虚拟摄像机** - 提示用户启动
4. ⚠️ **提醒开启OBS录制** - 需要手动在OBS中点击开始录制

## 🚀 快速开始

### 1. 使用自动录音录像脚本

```bash
./start_auto_recording.sh
```

或指定端口：

```bash
./start_auto_recording.sh 8082
```

### 2. 系统启动后的操作

脚本会自动完成以下步骤：

1. ✅ 清理旧进程
2. ✅ 加载v4l2loopback模块
3. ✅ 启动OBS（如果未运行）
4. ⏸️ **弹窗提示：请在OBS中启动虚拟摄像机**
   - 在OBS中点击：`工具 → 虚拟相机 → 启动`
   - 点击弹窗中的"是，已启动"
5. ✅ 启动后端服务（后台运行）
6. ✅ 自动启动语音识别
7. 🌐 自动打开Web界面
8. ⚠️ **提醒：请在OBS中点击【开始录制】按钮**

### 3. 确认OBS录制已开始

**重要！** 请确保在OBS中手动点击【开始录制】按钮：

```
OBS界面 → 控制区域 → 开始录制
```

或使用快捷键（默认可能是 `Ctrl+Shift+R`）

## 🔧 自动化级别对比

| 功能 | start.sh (原版) | start_auto_recording.sh (新版) |
|------|----------------|-------------------------------|
| 启动OBS | ✅ 自动 | ✅ 自动 |
| 启动虚拟摄像机 | ⏸️ 手动提示 | ⏸️ 手动提示 |
| 启动语音识别 | ❌ 需手动点击 | ✅ **自动启动** |
| 启动OBS录制 | ❌ 需手动点击 | ⚠️ **提醒用户** |
| 后台运行 | ❌ 前台阻塞 | ✅ **后台运行** |

## 📝 使用场景

### 场景1：完整录音录像（推荐）

适用于需要完整记录整个过程的场景。

```bash
# 1. 运行启动脚本
./start_auto_recording.sh

# 2. 在OBS弹窗中点击"启动虚拟摄像机"
# 3. 在OBS中点击"开始录制"按钮
# 4. 开始你的演讲/会议/实验
```

### 场景2：仅语音识别（无需OBS录制）

如果只需要语音转文字和关键时刻检测，不需要OBS的完整录制：

```bash
# 直接使用原版脚本即可
./start.sh
```

然后在Web界面中点击"开始录音"按钮。

## 🛠️ 进阶配置

### 1. 安装 obs-cli 实现完全自动化

如果希望OBS录制也能自动启动，可以安装 `obs-cli`：

```bash
# 方法1: 使用pip安装
pip install obs-cli

# 方法2: 或使用obs-websocket-py
pip install obs-websocket-py
```

安装后，脚本会自动尝试使用 `obs-cli` 启动录制。

### 2. OBS WebSocket 设置

如果使用 obs-websocket，需要在OBS中配置：

1. 打开OBS
2. `工具 → WebSocket服务器设置`
3. 启用WebSocket服务器
4. 设置端口（默认4444）
5. 可选：设置密码

### 3. 自定义启动参数

编辑 `.env.local` 文件配置更多参数：

```bash
# 语音识别API
DASHSCOPE_API_KEY=your_api_key_here

# OBS录制设置
OBS_WEBSOCKET_PORT=4444
OBS_WEBSOCKET_PASSWORD=your_password

# 自动录制延迟（秒）
AUTO_RECORDING_DELAY=15
```

## 🐛 故障排除

### 问题1：语音识别未自动启动

**可能原因：**
- 服务启动太慢，15秒等待时间不够
- 麦克风权限问题
- PyAudio未正确安装

**解决方案：**
```bash
# 检查服务日志
tail -f service_output.log

# 手动测试语音识别API
curl -X POST "http://localhost:8082/api/realtime_asr/start"

# 检查PyAudio
python3 -c "import pyaudio; print('PyAudio OK')"
```

### 问题2：OBS录制未开始

**解决方案：**
- 这是正常的，需要手动在OBS中点击"开始录制"
- 或安装 `obs-cli` 实现自动化
- 检查OBS的录制设置和保存路径

### 问题3：系统无法访问

**解决方案：**
```bash
# 检查进程是否在运行
ps aux | grep integrated_system.py

# 检查端口是否被占用
lsof -i:8082

# 查看完整日志
tail -f service_output.log
```

## 📊 监控和管理

### 查看运行状态

```bash
# 查看进程PID
cat service.pid

# 查看实时日志
tail -f service_output.log

# 检查端口
lsof -i:8082
```

### 停止系统

```bash
# 方法1: 使用PID
kill $(cat service.pid)

# 方法2: 杀掉所有相关进程
pkill -f integrated_system.py

# 方法3: 使用停止脚本
./stop.sh  # 如果存在
```

## 💡 最佳实践

1. **启动前检查**
   - 确保OBS已正确配置录制路径
   - 检查磁盘空间是否充足
   - 测试麦克风是否工作

2. **启动后验证**
   - 确认Web界面加载正常
   - 检查语音识别是否显示"录音中"
   - 确认OBS中显示"录制中"

3. **实验/会议期间**
   - 定期检查录制状态
   - 注意磁盘空间警告
   - 备份重要数据

4. **结束后**
   - 先停止OBS录制
   - 再停止系统服务
   - 检查录制文件完整性

## 🔗 相关文档

- [README.md](README.md) - 项目总体说明
- [📚 重要操作指令汇总.md](📚%20重要操作指令汇总.md) - 完整命令参考
- [INSTALL_UBUNTU.md](INSTALL_UBUNTU.md) - 安装指南

## ❓ 常见问题

**Q: 为什么不能完全自动化OBS录制？**

A: OBS的录制功能需要通过WebSocket API或插件控制。为了简化部署，当前脚本采用手动提醒方式。如需完全自动化，可安装 `obs-cli`。

**Q: 语音识别启动失败怎么办？**

A: 可以在Web界面中手动点击"开始录音"按钮，效果一样。自动启动只是为了方便。

**Q: 可以使用其他摄像头吗？**

A: 可以。启动时选择选项2（摄像头模式），或修改脚本中的 `--camera 0` 参数。

## 📞 技术支持

如有问题，请查看：
- 项目Issue: [GitHub Issues](https://github.com/your-repo/issues)
- 日志文件: `service_output.log`
- 调试日志: `debug_startup.log`
