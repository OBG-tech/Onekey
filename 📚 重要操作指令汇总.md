# 📚 智能视频分析系统 - 重要操作指令汇总

> **快速参考手册** - 包含所有关键命令和操作步骤  
> 最后更新：2025-12-16

## 🛑 紧急停止命令（快捷参考）

```bash
# 一键杀死所有进程 ⭐ 最常用
pkill -f integrated_system.py

# 强制释放端口（如提示端口占用）
lsof -ti:8082 | xargs kill -9
```

> 💡 详细说明见下方 [🔄 进程管理](#-进程管理) 部分

---

## 🚀 一键启动命令

### 方法 1：标准版启动（交互式选择）

```bash
cd /home/nucleus/onekey
./start.sh
```

**功能**：
- 自动检查环境
- 加载环境变量
- 清除代理设置
- 重新加载 v4l2loopback 模块
- 检查并启动 OBS 虚拟摄像机
- 提供交互式模式选择（OBS/摄像头/视频文件）

### 方法 2：AI 直播间版本启动

```bash
cd /home/nucleus/onekey
./start_live.sh
```

**功能**：
- 与标准版相同的系统检查
- 自动打开 AI 直播间页面（`integrated_final_live.html`）
- 支持实时 AI 解说和互动评论

### 方法 3：直接 Python 启动

```bash
cd /home/nucleus/onekey
source .venv/bin/activate
python3 integrated_system.py --obs --port 8082
```

---

## 🔧 系统安装与配置

### 完整安装（首次使用）

```bash
cd /home/nucleus/onekey
chmod +x install_ubuntu.sh
./install_ubuntu.sh
```

**安装内容**：
- 系统依赖（FFmpeg、PortAudio 等）
- Python 虚拟环境
- 所有 Python 包
- 摄像头权限配置
- v4l2loopback 模块

**安装完成后必须重新登录！**

```bash
logout
```

### 环境变量配置

创建或编辑 `.env.local` 文件：

```bash
cd /home/nucleus/onekey
nano .env.local
```

**配置内容**：

```bash
# 通义千问 API Key（阿里云 DashScope）
DASHSCOPE_API_KEY=sk-your-dashscope-api-key-here

# 可选：Claude API（如果使用 Claude）
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
LLM_PROVIDER=claude  # 或 qwen（默认）

# 可选：模型名称
LLM_MODEL=qwen-max
VISION_MODEL=qwen-vl-max-latest
```

---

## 📹 OBS 虚拟摄像头操作

### 加载/重新加载 v4l2loopback 模块

```bash
# 卸载旧模块
sudo modprobe -r v4l2loopback

# 加载新模块
sudo modprobe v4l2loopback devices=1 video_nr=8 card_label="OBS Virtual Camera" exclusive_caps=1
```

### 检查虚拟摄像机状态

```bash
# 检查模块是否加载
lsmod | grep v4l2loopback

# 检查摄像头设备
ls -l /dev/video*

# 查看详细信息
v4l2-ctl --list-devices
```

### OBS 中启动虚拟摄像机

1. 打开 OBS Studio
2. 菜单：**工具(Tools)** → **虚拟相机(Virtual Camera)**
3. 点击：**启动(Start)**

### 添加用户到 video 组（首次配置）

```bash
sudo usermod -a -G video $USER
logout  # 必须重新登录
```

---

## 🎮 启动模式选项

### 模式 1：OBS 虚拟摄像头模式（推荐）

```bash
python3 integrated_system.py --obs --port 8082
```

### 模式 2：本地摄像头模式

```bash
# 使用默认摄像头（/dev/video0）
python3 integrated_system.py --camera 0 --port 8082

# 使用其他摄像头
python3 integrated_system.py --camera 1 --port 8082
```

### 模式 3：视频文件模式

```bash
python3 integrated_system.py --video /path/to/video.mp4 --port 8082
```

### 高级选项组合

```bash
# 启用 AI 分析
python3 integrated_system.py --obs --ai --port 8082

# 禁用人脸识别（仅追踪）
python3 integrated_system.py --obs --no-face --port 8082

# 后台运行不自动打开浏览器
python3 integrated_system.py --obs --no-browser --port 8082
```

---

## 🔄 进程管理

### 查看运行中的进程

```bash
# 查看系统进程
ps aux | grep integrated_system.py

# 查看端口占用
lsof -i :8082
```

### 🛑 停止/杀死程序

#### ⚡ 快速停止（推荐）

```bash
# 方法 1：终端快捷键（如果终端仍在运行）
Ctrl + C

# 方法 2：一键杀死所有进程（最常用）⭐
pkill -f integrated_system.py

# 方法 3：强制释放端口（如果端口被占用）
lsof -ti:8082 | xargs kill -9
```

#### 🔄 完整清理并重启

```bash
# 杀掉所有相关进程
pkill -f integrated_system.py
sleep 1

# 释放端口
lsof -ti:8082 | xargs kill -9
sleep 1

# 重新启动
./start_live.sh
```

---

## 🌐 Web 界面访问

### 访问地址

```bash
# 标准版界面
http://localhost:8082/integrated final.html

# AI 直播间版本
http://localhost:8082/integrated_final_live.html

# 启动中心
http://localhost:8082/启动中心.html
```

### 自动打开浏览器

```bash
# Linux 系统
xdg-open "http://localhost:8082/integrated_final_live.html"

# 或使用
gnome-open "http://localhost:8082/integrated_final_live.html"
```

---

## 🐛 故障排查

### 检查虚拟环境

```bash
# 验证虚拟环境存在
ls -la /home/nucleus/onekey/.venv

# 激活虚拟环境
source /home/nucleus/onekey/.venv/bin/activate

# 检查已安装的包
pip list
```

### 检查依赖

```bash
# 核心库
python3 -c "import cv2; print(f'OpenCV: {cv2.__version__}')"
python3 -c "import numpy; print(f'NumPy: {numpy.__version__}')"
python3 -c "from ultralytics import YOLO; print('YOLO: OK')"

# AI 库
python3 -c "import openai; print('OpenAI: OK')"
python3 -c "import anthropic; print('Anthropic: OK')"

# 音频库
python3 -c "import pyaudio; print('PyAudio: OK')"

# 人脸识别（可选）
python3 -c "import insightface; print('InsightFace: OK')"
```

### 摄像头问题

```bash
# 列出所有摄像头
v4l2-ctl --list-devices

# 测试摄像头
ffplay /dev/video0

# 检查摄像头权限
ls -l /dev/video0
groups | grep video
```

### 端口被占用

```bash
# 查找占用端口的进程
lsof -i :8082

# 杀死占用进程
lsof -ti:8082 | xargs kill -9

# 或使用 netstat
netstat -tulpn | grep 8082
```

### 清除代理设置（API 调用失败时）

```bash
unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY all_proxy ALL_PROXY no_proxy NO_PROXY
```

### 查看系统日志

```bash
# 启动调试模式
python3 integrated_system.py --obs --verbose

# 查看日志文件
cat /home/nucleus/onekey/debug_startup.log

# 实时查看日志
tail -f /home/nucleus/onekey/debug_startup.log
```

---

## 🗄️ Git 仓库管理

### 清理大文件

```bash
cd /home/nucleus/onekey
chmod +x cleanup_git_large_files.sh
./cleanup_git_large_files.sh
```

**该脚本会**：
- 从 Git 历史中移除大文件
- 清理本地仓库
- 显示仓库大小

**清理后推送**：

```bash
# 检查工作区状态
git status

# 强制推送到远程（⚠️ 会重写历史！）
git push origin --force --all

# 推送标签
git push origin --force --tags
```

### 忽略大文件

编辑 `.gitignore`：

```bash
nano /home/nucleus/onekey/.gitignore
```

添加：

```
# 大模型文件
*.pt
*.pth
*.tar
*.onnx
pretrained_models/
models/
.venv/
integrated_data/
```

---

## 📦 数据目录结构

### 关键时刻存储

```bash
# 位置
/home/nucleus/onekey/integrated_data/key_moments/

# 包含
├── moments.json          # 关键时刻元数据
├── YYYYMMDD_HHMMSS/     # 时间戳命名的文件夹
│   ├── snapshot.jpg      # 快照图片
│   ├── clip.mp4          # 视频片段
│   └── audio.wav         # 音频（如有）
```

### 查看数据

```bash
# 查看所有关键时刻
ls -lh /home/nucleus/onekey/integrated_data/key_moments/

# 查看 moments.json
cat /home/nucleus/onekey/integrated_data/key_moments/moments.json | python3 -m json.tool
```

### 清理旧数据

```bash
# 删除特定日期的数据
rm -rf /home/nucleus/onekey/integrated_data/key_moments/20251215_*

# 清空所有数据（⚠️ 谨慎使用！）
rm -rf /home/nucleus/onekey/integrated_data/key_moments/*
```

---

## 🔐 权限和安全

### 使脚本可执行

```bash
chmod +x /home/nucleus/onekey/start.sh
chmod +x /home/nucleus/onekey/start_live.sh
chmod +x /home/nucleus/onekey/cleanup_git_large_files.sh
chmod +x /home/nucleus/onekey/install_ubuntu.sh
```

### 检查文件权限

```bash
ls -lh /home/nucleus/onekey/*.sh
```

### 设置环境变量文件权限

```bash
chmod 600 /home/nucleus/onekey/.env.local
```

---

## 📊 性能监控

### 系统资源监控

```bash
# 安装 htop（如未安装）
sudo apt install htop

# 实时监控
htop

# 查看 Python 进程
htop -p $(pgrep -f integrated_system.py)
```

### GPU 使用情况（如有 NVIDIA GPU）

```bash
# 实时监控
nvidia-smi -l 1

# 查看 GPU 占用
watch -n 1 nvidia-smi
```

---

## 🆘 快速修复命令

### 系统无法启动

```bash
# 完整重置流程
cd /home/nucleus/onekey
pkill -f integrated_system.py
lsof -ti:8082 | xargs kill -9
sudo modprobe -r v4l2loopback
sudo modprobe v4l2loopback devices=1 video_nr=8 card_label="OBS Virtual Camera" exclusive_caps=1
source .venv/bin/activate
./start_live.sh
```

### 虚拟摄像头不工作

```bash
# 重新加载模块
sudo modprobe -r v4l2loopback
sudo modprobe v4l2loopback devices=1 video_nr=8 card_label="OBS Virtual Camera" exclusive_caps=1

# 重启 OBS
pkill obs
obs &

# 在 OBS 中重新启动虚拟摄像机
```

### API 调用失败

```bash
# 检查 API Key
cat /home/nucleus/onekey/.env.local | grep API_KEY

# 清除代理
unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY all_proxy ALL_PROXY no_proxy NO_PROXY

# 测试 API
python3 -c "
import os
from openai import OpenAI
print(os.getenv('DASHSCOPE_API_KEY'))
"
```

### 重新安装依赖

```bash
cd /home/nucleus/onekey
source .venv/bin/activate
pip install --upgrade -r requirements.txt
```

---

## 📝 常用命令速查表

| 功能 | 命令 |
|------|------|
| 标准启动 | `./start.sh` |
| AI直播间启动 | `./start_live.sh` |
| **🛑 停止系统 ⭐** | `pkill -f integrated_system.py` |
| **🛑 强制释放端口** | `lsof -ti:8082 \| xargs kill -9` |
| 检查虚拟摄像头 | `lsmod \| grep v4l2loopback` |
| 重载v4l2模块 | `sudo modprobe v4l2loopback devices=1 video_nr=8` |
| 激活虚拟环境 | `source .venv/bin/activate` |
| 查看进程 | `ps aux \| grep integrated_system` |
| 查看端口 | `lsof -i :8082` |
| 打开浏览器 | `xdg-open http://localhost:8082/integrated_final_live.html` |
| 查看日志 | `tail -f debug_startup.log` |
| 清理Git大文件 | `./cleanup_git_large_files.sh` |

---

## 🎯 最佳实践

### 推荐启动流程

1. **确保环境配置**：
   ```bash
   cat .env.local  # 检查 API Key
   ```

2. **启动 OBS**（如使用虚拟摄像头）：
   ```bash
   obs &
   ```

3. **在 OBS 中启动虚拟摄像机**：
   - 工具 → 虚拟相机 → 启动

4. **启动系统**：
   ```bash
   ./start_live.sh
   ```

5. **验证运行**：
   - 检查浏览器是否自动打开
   - 查看视频流是否正常
   - 测试 AI 功能

### 日常维护

```bash
# 每天启动前
pkill -f integrated_system.py
lsof -ti:8082 | xargs kill -9

# 每周一次
cd /home/nucleus/onekey
git pull
source .venv/bin/activate
pip install --upgrade -r requirements.txt

# 每月一次
# 清理旧的关键时刻数据
find integrated_data/key_moments/ -type d -mtime +30 -exec rm -rf {} +
```

---

## 📞 获取帮助

### 查看系统帮助

```bash
python3 integrated_system.py --help
```

### 检查版本信息

```bash
python3 --version
pip list | grep -E "opencv|ultralytics|numpy"
```

### 调试模式启动

```bash
python3 integrated_system.py --obs --verbose --port 8082
```

---

## ⚡ 高级技巧

### 后台运行

```bash
nohup python3 integrated_system.py --obs --port 8082 > /tmp/integrated.log 2>&1 &
```

### 使用自定义端口

```bash
./start.sh 9090  # 使用端口 9090
```

### 同时运行多个实例

```bash
# 实例 1（端口 8082）
python3 integrated_system.py --camera 0 --port 8082 &

# 实例 2（端口 8083）
python3 integrated_system.py --video file.mp4 --port 8083 &
```

---

**🎉 享受智能视频分析的乐趣！**

Made with ❤️ by ZZH  
最后更新：2025-12-16
