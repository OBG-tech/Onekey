# 🎬 自动录音录像功能更新

## 📣 更新内容

为了解决**忘记开启录音导致没有完整录像**的问题，新增了自动化启动脚本和辅助工具。

## ✨ 新增文件

1. **`start_auto_recording.sh`** ⭐ 核心脚本
   - 自动启动语音识别
   - 自动启动OBS
   - 提示开启OBS虚拟摄像机和录制
   - 后台运行系统服务

2. **`自动录音录像指南.md`** 📖 详细文档
   - 完整使用说明
   - 故障排除指南
   - 最佳实践

3. **`obs_recording_control.sh`** 🛠️ OBS录制控制
   - 通过WebSocket控制OBS录制
   - 可以实现完全自动化（需要安装obs-websocket-py）

4. **`📖 启动脚本说明.md`** 📝 更新版
   - 添加了新脚本的说明
   - 提供脚本选择指南

## 🚀 快速使用

### 方法1：一键启动（推荐）

```bash
./start_auto_recording.sh
```

然后按照提示：
1. 在OBS弹窗中点击"启动虚拟摄像机"
2. 在OBS中手动点击"开始录制"按钮
3. 系统会自动启动语音识别

### 方法2：完全自动化（高级）

如果希望OBS录制也自动启动，需要先安装依赖：

```bash
pip install obs-websocket-py
```

然后在OBS中启用WebSocket：
- `工具 → obs-websocket设置 → 启用WebSocket服务器`

之后就可以使用：

```bash
# 启动系统（包含自动录音）
./start_auto_recording.sh

# 控制OBS录制
./obs_recording_control.sh
```

## 📊 功能对比

| 功能 | 原start.sh | 新start_auto_recording.sh |
|------|-----------|---------------------------|
| 启动OBS | ✅ | ✅ |
| 启动虚拟摄像机 | 手动提示 | 手动提示 |
| **启动语音识别** | ❌ **需手动点击** | ✅ **自动启动** |
| 启动OBS录制 | ❌ 需手动 | ⚠️ 提醒用户 |
| 后台运行 | ❌ 前台阻塞 | ✅ 后台运行 |
| 查看日志 | ✅ 实时显示 | 📄 写入service_output.log |

## 🎯 解决的问题

### 问题：忘记开启录音导致没有完整录像

**之前的流程：**
1. 运行 `start.sh`
2. 选择OBS模式
3. 等待系统启动
4. ❌ **忘记在Web界面点击"开始录音"**
5. ❌ **忘记在OBS中点击"开始录制"**
6. 结果：只有视频分析，没有语音转写和完整录像

**现在的流程（使用新脚本）：**
1. 运行 `start_auto_recording.sh`
2. 在弹窗中确认启动虚拟摄像机
3. ✅ **脚本自动启动语音识别**
4. ⚠️ **脚本提醒在OBS中开始录制**
5. 结果：完整的录音录像

## 📝 使用建议

### 适用场景

**使用 `start_auto_recording.sh` 当你需要：**
- ✅ 完整的录音录像记录
- ✅ 避免忘记开启录音
- ✅ 后台运行系统
- ✅ 一次性配置完所有功能

**使用 `start.sh` 当你需要：**
- ✅ 查看实时日志输出
- ✅ 调试系统问题
- ✅ 只进行视频分析（不需要录音）

### 注意事项

1. **OBS录制仍需手动开启**
   - 这是OBS的设计，需要用户确认录制参数
   - 如需完全自动化，可以安装 `obs-websocket-py`

2. **语音识别启动可能失败**
   - 如果服务启动较慢，自动启动可能失败
   - 此时可以在Web界面手动点击"开始录音"
   - 或者等待更长时间后运行 `test_auto_recording.sh`

3. **检查磁盘空间**
   - 录音录像会占用大量空间
   - 建议定期清理旧数据

## 🐛 故障排除

### 语音识别未自动启动

**检查服务状态：**
```bash
tail -f service_output.log
```

**手动测试API：**
```bash
curl -X POST "http://localhost:8083/api/realtime_asr/start"
```

**手动启动：**
在Web界面点击"开始录音"按钮

### OBS录制未开始

**解决方案：**
1. 在OBS界面手动点击"开始录制"
2. 或安装 `obs-websocket-py` 并使用 `obs_recording_control.sh`

## 📚 相关文档

- [自动录音录像指南.md](自动录音录像指南.md) - 完整使用指南
- [📖 启动脚本说明.md](📖%20启动脚本说明.md) - 所有启动脚本对比
- [📚 重要操作指令汇总.md](📚%20重要操作指令汇总.md) - 完整命令参考
- [README.md](README.md) - 项目总体说明

## 🔄 迁移指南

如果你之前一直使用 `start.sh`，现在想切换到自动录音录像：

```bash
# 旧方式
./start.sh
# 然后在Web界面点击"开始录音"
# 然后在OBS中点击"开始录制"

# 新方式（推荐）
./start_auto_recording.sh
# 系统会自动启动录音
# 只需在OBS中点击"开始录制"
```

## ✅ 验证清单

启动后，请确认以下项目：

- [ ] Web界面正常打开
- [ ] 页面显示"录音中"（而不是"点击开始录音"）
- [ ] OBS窗口显示"录制中"
- [ ] 说话时能看到实时语音转文字
- [ ] 人脸识别/追踪正常工作

## 🤝 反馈

如有问题或建议，请：
- 查看日志：`tail -f service_output.log`
- 查看指南：[自动录音录像指南.md](自动录音录像指南.md)
- 提交Issue：描述问题和日志信息

---

**更新日期：** 2026-01-04  
**版本：** v1.0
