# 数据归档工具使用指南

## 📋 功能说明

**`archive_data.py`** - 按日期自动归档所有历史数据

### 归档内容

1. **视频流** - 关键时刻视频片段（.mp4）
2. **音频流** - 录音文件
3. **关键时刻** - 图片、视频、上下文
4. **对话流** - 转录文本、会议笔记
5. **人物信息** - 人脸快照、人脸数据库
6. **分析结果** - AI分析数据
7. **关键帧** - 提取的关键帧
8. **日志文件** - button_log.txt, debug_startup.log等

### 归档结构

```
archives/
├── 2024-12-15/
│   ├── key_moments/
│   │   ├── anchor_1765559797_1385.jpg
│   │   ├── anchor_1765559797_1385.mp4
│   │   └── ...
│   ├── audio/
│   ├── transcripts/
│   ├── snapshots/
│   │   ├── person_1/
│   │   ├── person_2/
│   │   └── ...
│   ├── logs/
│   └── ...
├── 2024-12-16/
└── ...
```

## 🚀 使用方法

### 1. 试运行（推荐第一次使用）

查看将要归档的文件，但不实际复制：

```bash
cd /home/nucleus/onekey
python3 archive_data.py --dry-run
```

### 2. 执行归档

实际复制文件到归档目录：

```bash
python3 archive_data.py
```

### 3. 自定义选项

```bash
# 指定自定义输出目录
python3 archive_data.py --output /path/to/my/archives

# 指定自定义数据源
python3 archive_data.py --source /path/to/data

# 组合使用
python3 archive_data.py --source integrated_data --output backup/2024 --dry-run
```

## 📊 输出示例

```
============================================================
智能视频分析系统 - 数据归档工具
============================================================

⚠️  试运行模式（不会实际复制文件）

📁 处理类别: key_moments
  📅 2024-12-15: 45 个文件
  📅 2024-12-16: 32 个文件

📁 处理类别: audio
  📅 2024-12-15: 8 个文件

📁 处理类别: transcripts
  📅 2024-12-15: 12 个文件
  📅 2024-12-16: 5 个文件

...

============================================================
归档完成统计
============================================================
总文件数: 234
已归档: 234
跳过: 0
错误: 0

✅ 归档保存在: /home/nucleus/onekey/archives
```

## ⚙️ 工作原理

### 日期提取

1. **从文件名提取**：
   - `anchor_1765559797_1385.jpg` → 提取 `1765559797` → 转换为日期
   - `ai_1765506604_4472.jpg` → 提取 `1765506604` → 转换为日期

2. **从文件修改时间**：
   - 如果文件名没有时间戳，使用文件最后修改时间

### 文件处理

- **复制而非移动**：原文件保持不变，安全第一
- **保留目录结构**：如 `snapshots/person_1/` 结构会保留
- **避免重复**：已存在的文件会被覆盖

## 🔧 高级用法

### 定期自动归档

创建定时任务（crontab）每周归档一次：

```bash
# 编辑定时任务
crontab -e

# 添加任务：每周日凌晨2点执行归档
0 2 * * 0 cd /home/nucleus/onekey && /usr/bin/python3 archive_data.py >> /tmp/archive.log 2>&1
```

### 归档后压缩

```bash
# 归档完成后压缩特定日期
cd archives
tar -czf 2024-12-15.tar.gz 2024-12-15/

# 删除原始文件夹（可选）
rm -rf 2024-12-15/
```

### 批量处理

```bash
# 归档所有数据并压缩
python3 archive_data.py
cd archives
for dir in 2024-*/; do
    tar -czf "${dir%/}.tar.gz" "$dir"
    echo "已压缩: ${dir%/}.tar.gz"
done
```

## ⚠️ 注意事项

1. **磁盘空间**：归档会复制文件，确保有足够空间
2. **首次使用**：先用 `--dry-run` 测试
3. **原始数据**：归档是复制，原始数据不会删除
4. **权限**：确保对数据目录有读权限

## 🆘 故障排查

### 问题：无法找到文件的日期

**原因**：文件名不包含时间戳  
**解决**：脚本会自动使用文件修改时间

### 问题：归档文件太大

**解决**：
```bash
# 归档后逐个日期压缩
cd archives
tar -czf 2024-12-15.tar.gz 2024-12-15/
```

### 问题：想恢复某个日期的数据

**解决**：
```bash
# 从归档中复制回来
cp -r archives/2024-12-15/key_moments/* integrated_data/key_moments/
```

## 📝 维护建议

- **每周归档一次**：保持数据整洁
- **每月压缩归档**：节省空间
- **重要数据备份**：将归档复制到外部存储

## 🔗 相关命令

查看归档统计：
```bash
# 查看每个日期的文件数
cd archives
du -sh */
```

查看最大的归档目录：
```bash
du -sh archives/*/ | sort -h
```
