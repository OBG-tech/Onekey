# 1211主程序 - 使用说明

## 📂 文件结构

```
1211主程序/
├── integrated_system.py          # 主程序（后端服务器）
├── key_moments_manager.py        # 关键时刻管理模块
├── LAUNCH_GUI.py                 # GUI启动器（可选）
├── 🔄 一键重启系统.command       # ⭐ 主启动脚本
├── web/                          # 前端网页界面
│   └── integrated.html
├── yolo11n.pt                    # YOLO模型
├── yolov8n-seg.pt               # YOLO分割模型
├── integrated_data/              # 数据存储目录
│   └── key_moments/             # 关键时刻保存位置
└── README.md                     # 说明文档
```

## 🚀 使用方法

### 方法1：一键启动（推荐）⭐
双击 `🔄 一键重启系统.command`

### 方法2：命令行启动
```bash
cd 1211主程序
source ../.venv/bin/activate  # 需要在毕设目录下有.venv
python3 integrated_system.py
```

## 📝 注意事项

1. **虚拟环境：** 需要在上级目录（毕设/）有 `.venv` 虚拟环境
2. **摄像头权限：** 首次运行会请求摄像头权限
3. **浏览器访问：** 系统启动后访问 http://localhost:8080
4. **AI功能：** 支持 Qwen 和 Claude Haiku 4.5 两种提供者

### AI 配置选项（可选）

**使用 Qwen（默认）：**
```bash
export DASHSCOPE_API_KEY="sk-your-key-here"
# 或在 🔄 一键重启系统.command 中设置
```

**使用 Claude Haiku 4.5：**
```bash
export LLM_PROVIDER="claude"
export ANTHROPIC_API_KEY="sk-ant-your-key-here"
# 可选：指定模型
export LLM_MODEL="claude-3-5-haiku-20241022"
export VISION_MODEL="claude-3-5-haiku-20241022"
```

**环境变量说明：**
- `LLM_PROVIDER`: `qwen`(默认) 或 `claude`
- `DASHSCOPE_API_KEY`: 阿里云 DashScope API Key
- `ANTHROPIC_API_KEY` / `CLAUDE_API_KEY`: Claude API Key
- `LLM_MODEL`: 文本生成模型名
- `VISION_MODEL`: 视觉分析模型名 (仅 Qwen 支持)

## 🎯 主要功能

- ✅ 实时视频流显示
- ✅ 人物检测追踪
- ✅ 人脸识别（需InsightFace）
- ✅ 关键时刻标记
- ✅ 视频片段保存
- ✅ 音频提取合并（视频文件模式）
- ✅ 语音识别（需配置 DashScope）
- ✅ AI分析（支持 Qwen 和 Claude Haiku 4.5）

## 🔧 依赖安装

主要依赖包（在上级目录的.venv中）：
```bash
opencv-python
ultralytics
numpy
insightface (可选)
```

## 📞 问题反馈

如有问题，请检查：
1. 虚拟环境是否激活
2. 摄像头是否可用
3. 端口8080是否被占用
4. Python版本 >= 3.8
