# 📹 视频文件存储位置说明

## 📂 存储目录结构

系统会在多个位置保存视频和相关数据：

```
onekey/
├── integrated_data/              # 主数据目录
│   ├── key_moments/              # 🎬 关键时刻视频（最重要）
│   │   ├── anchor_*.mp4          # 手动标记的视频片段
│   │   ├── multimodal_*.mp4      # AI检测的视频片段
│   │   ├── moment_*.json         # 关键时刻元数据
│   │   └── frame_*.jpg           # 关键帧图片
│   │
│   ├── audio/                    # 🎤 音频录制文件
│   │   └── recording_*.wav       # 麦克风录音
│   │
│   ├── key_frames/               # 📸 关键帧图片
│   │   └── keyframe_*.jpg        # 检测到人物时的关键帧
│   │
│   ├── face_database/            # 👤 人脸数据库
│   │   └── person_*.jpg          # 人物头像
│   │
│   ├── snapshots/                # 📷 快照图片
│   │   └── person_*/             # 按人物分类的快照
│   │
│   ├── transcripts/              # 📝 语音转写文本
│   │   └── transcript_*.txt      # ASR转写结果
│   │
│   └── logs/                     # 📋 日志文件
│       └── analysis.log          # 分析日志
│
└── (OBS录制的视频保存在OBS设置的路径)
```

## 🎬 关键时刻视频详解

### 保存位置
```
integrated_data/key_moments/
```

### 视频类型

#### 1. 手动标记视频 (`anchor_*.mp4`)
- **来源：** 用户按空格键或ESP32按钮手动标记
- **时长：** 40秒（标记点前20秒 + 后20秒）
- **命名格式：** `anchor_时间戳_帧号.mp4`
- **示例：** `anchor_1766158982_35937.mp4`

#### 2. AI检测视频 (`multimodal_*.mp4`)
- **来源：** AI多模态分析自动检测
- **时长：** 30秒（可在环境变量中配置）
- **命名格式：** `multimodal_时间戳_帧号.mp4`
- **示例：** `multimodal_1766224453_193274.mp4`

### 关联文件

每个视频都有对应的元数据文件：

#### moment_*.json（元数据）
```json
{
  "id": "anchor_1766158982_35937",
  "timestamp": 1766158982,
  "frame_number": 35937,
  "video_path": "integrated_data/key_moments/anchor_1766158982_35937.mp4",
  "frame_path": "integrated_data/key_moments/frame_1766158982_35937.jpg",
  "description": "AI生成的描述",
  "transcript": "语音转写内容",
  "importance": 0.85,
  "type": "user_anchor",  // 或 "ai_detected"
  "created_at": "2025-12-19T15:23:02"
}
```

## 🎥 OBS录制视频

### 保存位置
**不在项目目录中！** 而是保存在OBS设置的路径。

### 查看/修改OBS录制路径

1. 打开OBS
2. `文件 → 设置 → 输出`
3. 查看"录像"部分的"录像路径"

**常见默认路径：**
- **Linux:** `~/Videos/` 或 `~/视频/`
- **macOS:** `~/Movies/`
- **Windows:** `C:\Users\用户名\Videos\`

### OBS录制文件特点
- **完整录像：** 从点击"开始录制"到"停止录制"的完整视频
- **文件名：** 通常包含日期时间，如 `2026-01-04 23-13-34.mp4`
- **时长：** 完整的实验/会议时长（可能数小时）
- **大小：** 通常很大（几GB到几十GB）

## 📊 存储空间占用

### 典型占用情况（1小时实验）

| 类型 | 位置 | 单个大小 | 数量 | 总大小 |
|------|------|---------|------|--------|
| OBS完整录像 | OBS设置路径 | 1-3 GB | 1 | 1-3 GB |
| 关键时刻视频 | `key_moments/` | 5-20 MB | 10-50个 | 50-1000 MB |
| 关键帧图片 | `key_frames/` | 100-500 KB | 1000+ | 100-500 MB |
| 人脸数据库 | `face_database/` | 50-200 KB | 10-50 | 1-10 MB |
| 音频录制 | `audio/` | 10-50 MB | 1 | 10-50 MB |
| 转写文本 | `transcripts/` | 10-100 KB | 多个 | 1-5 MB |

**总计：** 约 2-5 GB（包含OBS录像）

## 🔍 查找视频文件

### 查找关键时刻视频
```bash
cd /home/nucleus/onekey

# 列出所有关键时刻视频
find integrated_data/key_moments -name "*.mp4"

# 按时间排序
find integrated_data/key_moments -name "*.mp4" -printf '%T@ %p\n' | sort -n | awk '{print $2}'

# 统计数量
find integrated_data/key_moments -name "*.mp4" | wc -l

# 查看总大小
du -sh integrated_data/key_moments/
```

### 查找OBS录像
```bash
# 查看OBS配置（如果有）
grep -r "录像路径" ~/.config/obs-studio/ 2>/dev/null

# 常见位置
ls -lh ~/Videos/*.mp4
ls -lh ~/视频/*.mp4
```

### 快速统计
```bash
# 查看所有数据目录大小
du -sh integrated_data/*

# 查看视频文件数量和大小
echo "关键时刻视频:"
find integrated_data/key_moments -name "*.mp4" -exec du -ch {} + | tail -1
```

## 📦 备份和转移

### 备份关键时刻数据
```bash
# 方法1: 使用内置打包脚本
./pack_moments_data.sh

# 方法2: 手动打包
tar -czf moments_backup_$(date +%Y%m%d_%H%M%S).tar.gz integrated_data/key_moments/

# 方法3: 只备份视频文件
mkdir -p backup/videos
cp integrated_data/key_moments/*.mp4 backup/videos/
```

### 清理旧数据
```bash
# 使用归档脚本（推荐）
python3 archive_data.py

# 手动清理（谨慎！）
# 删除7天前的关键时刻视频
find integrated_data/key_moments -name "*.mp4" -mtime +7 -delete
```

### 转移OBS录像
```bash
# 1. 找到OBS录像位置
# 在OBS中：文件 → 设置 → 输出 → 录像路径

# 2. 复制到指定位置
cp ~/Videos/*.mp4 /path/to/backup/

# 3. 或使用rsync
rsync -av --progress ~/Videos/*.mp4 /path/to/backup/
```

## 🧹 清理建议

### 定期清理策略

**每周清理：**
```bash
# 删除关键帧图片（保留30天）
find integrated_data/key_frames -name "*.jpg" -mtime +30 -delete

# 压缩旧的转写文本
find integrated_data/transcripts -name "*.txt" -mtime +30 -exec gzip {} \;
```

**每月清理：**
```bash
# 归档重要数据
python3 archive_data.py

# 删除低重要性的关键时刻
# （需要手动筛选，或编写脚本根据importance字段删除）
```

### 保留重要数据
```bash
# 备份高重要性的关键时刻
mkdir -p important_moments
find integrated_data/key_moments -name "moment_*.json" -exec grep -l '"importance": [0-9]\.[89]' {} \; | \
while read json; do
    video="${json%.json}.mp4"
    [ -f "$video" ] && cp "$video" important_moments/
done
```

## 🔗 与Web界面的关联

### Web界面中的视频
- **关键时刻卡片：** 显示的是 `key_moments/` 中的视频
- **点击播放：** 直接从服务器流式播放
- **下载：** 右键可以下载单个视频文件

### 访问方式
```
http://localhost:8082/integrated_data/key_moments/anchor_1766158982_35937.mp4
```

## 📝 最佳实践

### 1. 定期备份
```bash
# 创建每周备份任务
0 2 * * 0 cd /home/nucleus/onekey && ./pack_moments_data.sh
```

### 2. 监控磁盘空间
```bash
# 检查磁盘使用情况
df -h .

# 查看最大的文件
du -ah integrated_data | sort -rh | head -20
```

### 3. OBS录制设置建议
- **格式：** MP4（兼容性好）或 MKV（更安全，崩溃不会损坏）
- **编码器：** H.264（x264）
- **比特率：** 5000-8000 Kbps
- **分段录制：** 建议启用（避免单个文件过大）

### 4. 自动清理脚本示例
```bash
#!/bin/bash
# auto_cleanup.sh - 自动清理脚本

# 删除30天前的关键帧
find integrated_data/key_frames -name "*.jpg" -mtime +30 -delete

# 删除90天前的低重要性关键时刻（importance < 0.5）
# （需要自己实现筛选逻辑）

# 压缩旧的转写文本
find integrated_data/transcripts -name "*.txt" -mtime +30 -exec gzip {} \;

# 删除7天前的音频录制
find integrated_data/audio -name "*.wav" -mtime +7 -delete

echo "清理完成: $(date)"
```

## ❓ 常见问题

### Q: 找不到OBS录制的视频？
**A:** 在OBS中查看设置的录制路径：`文件 → 设置 → 输出 → 录像路径`

### Q: 关键时刻视频在哪里？
**A:** `integrated_data/key_moments/` 目录

### Q: 如何只保存重要的视频？
**A:** 查看 `moment_*.json` 中的 `importance` 字段，保留高分视频

### Q: 磁盘空间不够怎么办？
**A:** 
1. 转移OBS录像到外部硬盘
2. 删除低重要性的关键时刻
3. 压缩或删除关键帧图片
4. 使用 `archive_data.py` 归档旧数据

### Q: 如何批量下载关键时刻视频？
**A:**
```bash
# 复制到桌面
mkdir -p ~/Desktop/key_moments_backup
cp integrated_data/key_moments/*.mp4 ~/Desktop/key_moments_backup/

# 或打包
tar -czf key_moments_$(date +%Y%m%d).tar.gz integrated_data/key_moments/*.mp4
```

## 🔗 相关文档

- [ARCHIVE_GUIDE.md](ARCHIVE_GUIDE.md) - 数据归档指南
- [自动录音录像指南.md](自动录音录像指南.md) - 录制功能说明
- [📚 重要操作指令汇总.md](📚%20重要操作指令汇总.md) - 完整命令参考

---

**提示：** OBS的完整录像和系统的关键时刻视频是两套独立的存储系统，建议都妥善保管！
