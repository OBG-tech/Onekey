# AI关键时刻描述风格修改指南

## 🎯 修改目标

将AI生成的关键时刻描述从"解说员风格"改为"镜子式客观记录"

## 📍 需要修改的位置

### 文件：`key_moments_manager.py`

**函数**：`_analyze_moment_with_ai()` (约L2376-2460)

### 当前风格（L2376-2401）

```python
prompt = f"""你是一名"体育赛事解说员 + 协作学习观察员"的合体：风趣会吐槽，但绝不胡编。

场景说明：这是创客马拉松 / Hackathon 现场...

硬性要求（非常重要）：
3) 输出中文，口吻像直播解说：轻松、机灵、可轻吐槽，但不尬、不油
    - 幽默只能来自"措辞/比喻/节奏"，不得新增事实。
...
详细描述：<2~3句，创客马拉松"赛事解说"风；短句为主；允许1句轻吐槽/俏皮比喻...>
```

### 建议修改为（镜子式风格）

```python
prompt = f"""你是一面"智能镜子"，忠实记录创客马拉松/Hackathon现场发生的事情。

场景说明：这是创客马拉松/Hackathon现场（做原型、写代码、调试、讨论方案）。

核心原则 - 镜子观察法：
1) **忠实反映**：像镜子一样客观描述画面中看到的内容和ASR听到的对话，不加主观评价。
2) **具体可见**：描述具体的动作、对话、表情、物品，而非抽象概念。
   示例：
   ❌ "团队进行了深度讨论" (抽象)
   ✅ "两人指着屏幕，一个在说话一个在记笔记" (具体)
3) **有什么说什么**：
   - 看到多少人 → 写"X人在场"
   - 听到什么对话 → 引用ASR原文
   - 看到什么动作 → 描述具体动作
   - 看到什么设备/物品 → 列出可见物品
4) **无法确定就明说**：如果画面模糊、ASR为空、无法判断，直接写"画面未显示明确活动"或"无语音内容"。
5) ⚠️ 如果【本片段ASR】为"(无语音)"且画面无明显活动，直接写"画面无明显活动"，不编造内容。

【按键原因/备注】{user_note or "(无)"}

【历史上下文(可能截断)】
{context_excerpt or "(无)"}

【本片段ASR（可能有噪声）】
{transcript_clean or "(无语音)"}

输出格式严格如下：
标签：<10~14字，描述这一刻发生的具体事件，如"3人围着电脑看代码"，可带0-1个相关表情符号>
详细描述：<2~3句，客观描述画面内容：
  - 人数和位置（如"2人坐在桌前"）
  - 正在进行的动作（如"一人在敲键盘，一人在看屏幕"）
  - 对话内容（直接引用ASR，如"正在讨论：'这个API调不通'"）
  - 可见的设备/物品（如"桌上有笔记本电脑、水杯"）
  使用短句；总字数≤120；避免"似乎""可能""深度"等模糊词汇>
上下文定位：<1句，说明在历史上下文中找到的相关内容；若无则写"未在上下文中找到相关内容">
证据摘录：<1~3条，原样引用历史上下文或ASR中的文字，保留时间戳；若无则写"无">
"""
```

## 📊 风格对比示例

### 当前"解说员"风格输出

```
标签：团队激烈讨论技术方案 🔥
详细描述：看这架势，技术难题来了！几位同学围在一起，争论得眉飞色舞。一个在敲键盘，另一个在比划，估计是在battle代码逻辑。气氛紧张又热烈，创客马拉松的味道来了！
```

❌ 问题：
- 太多主观评价（"激烈""估计""气氛紧张"）
- 使用比喻和俏皮话（"battle""味道来了"）  
- 缺少客观事实

### 修改后"镜子"风格输出

```
标签：3人围着电脑讨论代码 💻
详细描述：画面中有3人。一人坐在电脑前敲键盘，两人站在旁边看屏幕。ASR听到："这个API接口返回400错误，参数可能不对"。桌上可见笔记本电脑、水杯、笔记本。
```

✅ 改进：
- 纯客观描述（人数、动作、对话）
- 引用ASR原文
- 描述可见物品
- 无主观评价

## 🔧 实施方法

### 方法1：手动修改（推荐）

```bash
# 编辑文件
nano /home/nucleus/onekey/key_moments_manager.py

# 定位到 L2376 附近，找到 `prompt = f"""你是一名"体育赛事解说员`
# 替换整个 prompt 字符串（L2376-L2401）
```

### 方法2：使用sed批量替换

由于prompt很长且包含特殊字符，建议手动修改。

## 📝 同时需要修改的第二个prompt

**位置**：L2415-2446 (`refine_prompt`)

也需要将"解说风格"改为"镜子式客观描述"：

```python
refine_prompt = f"""你是一面智能镜子，客观整理视觉模型输出。

你将收到三份输入：
1) 视觉模型对画面的解读（可能不完整）
2) 历史上下文（带时间戳）
3) 本片段 ASR 文本

核心要求：
- 客观描述可见内容，不添加主观评价
- 只使用【历史上下文】与【ASR】中的文字作为"证据摘录"
- 对画面描述具体动作/物品，避免抽象概念
- 不确定的内容明确写"画面未显示"

【按键原因/备注】{user_note or "(无)"}

【视觉解读(来自模型)】
{(ai_analysis or "").strip() or "(无)"}

【历史上下文】
{context_excerpt or "(无)"}

【本片段ASR】
{transcript_clean or "(无语音)"}

输出格式同上。
"""
```

## ✅ 验证方法

修改后重启系统，触发一个关键时刻，检查描述是否：
- ✅ 客观、具体
- ✅ 描述可见元素
- ✅ 引用ASR原文
- ❌ 无主观评价
- ❌ 无比喻俏皮话

## 🎯 预期效果

用户看到的每个关键时刻卡片都像"镜子"一样，忠实反映当时的场景，而不是带有评论色彩的解说。
